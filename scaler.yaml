---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: stackablescalers.autoscaling.stackable.tech
spec:
  group: autoscaling.stackable.tech
  names:
    categories: []
    kind: StackableScaler
    plural: stackablescalers
    shortNames: []
    singular: stackablescaler
  scope: Namespaced
  versions:
    - additionalPrinterColumns: []
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Auto-generated derived type for StackableScalerSpec via `CustomResource`
          properties:
            spec:
              description: |-
                A StackableScaler exposes a /scale subresource so that a Kubernetes
                HorizontalPodAutoscaler can target it instead of a StatefulSet directly.
              properties:
                clusterRef:
                  description: Reference to the Stackable cluster resource this scaler manages.
                  properties:
                    kind:
                      type: string
                    name:
                      type: string
                  required:
                    - kind
                    - name
                  type: object
                replicas:
                  description: |-
                    Desired replica count. Written by the HPA via the /scale subresource.
                    Only takes effect when the referenced roleGroup has `replicas: 0`.
                  format: int32
                  type: integer
                role:
                  description: The role within the cluster (e.g. `nodes`).
                  type: string
                roleGroup:
                  description: The role group within the role (e.g. `default`).
                  type: string
              required:
                - clusterRef
                - replicas
                - role
                - roleGroup
              type: object
            status:
              description: Status of a StackableScaler.
              nullable: true
              properties:
                currentState:
                  description: The current state of the scaler, including when it last changed.
                  nullable: true
                  properties:
                    lastTransitionTime:
                      description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                      format: date-time
                      type: string
                    stage:
                      properties:
                        details:
                          properties:
                            failedAt:
                              description: Which stage of a scaling operation failed.
                              enum:
                                - preScaling
                                - scaling
                                - postScaling
                              type: string
                            reason:
                              type: string
                          type: object
                        stage:
                          enum:
                            - idle
                            - preScaling
                            - scaling
                            - postScaling
                            - failed
                          type: string
                      required:
                        - stage
                      type: object
                  required:
                    - lastTransitionTime
                    - stage
                  type: object
                desiredReplicas:
                  format: int32
                  nullable: true
                  type: integer
                replicas:
                  format: int32
                  type: integer
                selector:
                  nullable: true
                  type: string
              required:
                - replicas
              type: object
          required:
            - spec
          title: StackableScaler
          type: object
      served: true
      storage: true
      subresources:
        scale:
          labelSelectorPath: .status.selector
          specReplicasPath: .spec.replicas
          statusReplicasPath: .status.replicas
        status: {}